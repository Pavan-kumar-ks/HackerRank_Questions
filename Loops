The provided code stub reads and integer, n, from STDIN. 
For all non-negative integers i<n , print i*i.

The logic which I used is as follows:

if __name__ == '__main__':
    n = int(input())
    for i in range(n):
        print(i*i)
