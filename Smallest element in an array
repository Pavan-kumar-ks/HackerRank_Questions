Problem: To find the smallest element in an unsorted array.

#include<stdio.h>
#include<stdlib.h>

int main()
{
    int i,a[10],n;
    printf("enter the size of the array\n");
    scanf("%d",&n);
    printf("enter the array\n");
    for(i=0;i<n;i++)
    {
        scanf("%d",&a[i]);
    }
    int small = a[0];
    for(i=0;i<n;i++)
    {
        if(a[i]<small)
        {
            small = a[i];
        }
    }
    printf("the smallest element is %d",small);
}

Hope this helps :)
